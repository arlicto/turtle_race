# Turtle Race

A simple turtle racing game built with Python's `turtle` module.

## How to Run

```bash
python main.py
```

Each turtle moves a random distance per step. The first to cross the finish line wins.
